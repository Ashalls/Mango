# Mango — Full Application Audit (2026-07-12)

Reviewers: three Fable agents (renderer/UI, main-process/security, data-integrity/state),
consolidated. A second independent pass by GPT-5.6 Sol (`/full-review`) is appended in the
"Sol pass" section at the bottom.

Severity key: **Critical** = data loss / corruption / security. **High** / **Medium** /
**Low** as noted. File:line refs are against the tree at commit `32050f2` (v0.7.4).

---

## 0. Priority: the vanishing document grid ("tray grid disappears until right-click")

> **✅ ADDRESSED (2026-07-12).** Fixed on three fronts: (1) `MainPanel.tsx` now keeps
> `DocumentTable` at one stable tree position (the split/non-split layouts were separate JSX
> branches, so ag-grid was unmounted+remounted on every row click — the prime moment it latched a
> 0-height viewport); (2) the self-heal is now **state-based** — on any resize it repaints when the
> wrapper is sized but ag-grid has rendered zero row nodes (`api.getRenderedNodes().length === 0`),
> plus an `onGridSizeChanged` companion — dropping the fragile `prevHeight === 0` gate the coalesced
> ResizeObserver defeated; (3) `getRowId` no longer uses the nonexistent `params.rowIndex` and now
> gives `_id`-less / object-`_id` rows stable ids (a separate blank mode right-click did NOT fix).
> Also moved the add-row `useState` hooks above the early return (Rules-of-Hooks fix). Still
> recommended as follow-up: memoize `columnDefs`/`rowData` and fix the no-selector store
> subscriptions (perf; removes the re-render storm) — deferred to avoid the closure-staleness risk.

Root cause is **not** primarily the height-collapse the previous two fixes chased. Two things combine:

1. **Right-click heals it because *any* re-render heals it.** `columnDefs` is rebuilt as a fresh
   array on every render (`DocumentTable.tsx:244`, unmemoized). Right-click → `onCellContextMenu`
   → `setContextDoc` → re-render → new column-defs identity → ag-grid re-lays-out and repaints.
2. **ag-grid latches a transient 0-height viewport the self-heal misses.** The canary at
   `DocumentTable.tsx:212` gates on `prevHeight === 0`, but ResizeObserver coalesces a
   collapse→restore within one frame into a single *positive* entry, so the 0 is never witnessed
   and the `[DocumentTable] grid wrapper collapsed` warning never fires.

Confirmed triggers:
- DocumentTable is **fully remounted on every row click** — the two `MainPanel.tsx:170-196`
  ternary branches have mismatched element types at child slot 0.
- The split pane at `MainPanel.tsx:178` reintroduces the `%`-height-of-a-flex-grown-item Chromium
  bug the comments warn about (moved up one level, not removed).
- No-selector `useTabStore()` at `DocumentTable.tsx:155` → re-render storm (per Claude streaming
  token, per drag pointermove) that randomly masks/heals the bug.

Separate blank-grid mode right-click does **not** fix: `getRowId` at `DocumentTable.tsx:553` uses
`params.rowIndex` (does not exist on `GetRowIdParams`); `_id`-less results get id `"undefined"`
for every row and object `_id`s get `"[object Object]"`, so ag-grid merges/drops duplicate-id rows.

**Durable fix, in order:**
1. `useMemo` `columnDefs` + `rowData` (deps: documents, isReadOnly, tab.isView, addRowOpen,
   addRowValues) — removes the accidental heal and the column churn.
2. Keep DocumentTable at one stable tree position: render the split container unconditionally,
   vary only pane style. Replace the `%`-height pane with a measured pixel height / flex-basis.
3. State-based self-heal (drop the `prevHeight===0` gate): on any RO tick and in
   `onGridSizeChanged`, if `height>0 && api.getDisplayedRowCount()===0 && docCount>0` →
   `resetRowHeights(); redrawRows()`.
4. Fix `getRowId`: guard null, `JSON.stringify` object `_id`s, counter fallback for `_id`-less docs.

---

## 1. Critical

- **C1. ✅ RESOLVED (2026-07-12).** _Queries & writes hit the globally "active" connection, not the
  tab's._ Fixed by threading each tab's `connectionId` through every `trpc.query.*`/`trpc.mutation.*`
  input into `getDb(name, connectionId)`, across the query/mutation routers, actions, and all
  renderer callers (find, count, documentSource, aggregate/stage-preview, explain, insert/update/
  delete one+many, and the bulk dialogs). `checkReadOnly(connectionId?)` now guards the same
  connection the write targets. Follow-up not covered here: the `exportImport` router still uses the
  active connection for the actual export stream (read-only, no data-loss risk).
- **C2. ✅ RESOLVED (2026-07-12).** _Aggregation `$out`/`$merge` bypassed every write guard._ The
  aggregate actions now reject a pipeline containing `$out`/`$merge` when the target connection is
  read-only (`pipelineHasWriteStage` + `checkReadOnly` in `actions/query.ts`, covering both the
  tRPC `query.aggregate`/`aggregateWithStagePreview` and the MCP tools), and the MCP
  `mongo_aggregate`/`mongo_aggregate_preview` handlers additionally apply the production-aware
  `checkWriteAccess` guard used by the write tools.
- **C3. ✅ RESOLVED (2026-07-12).** _TreeView pending edits were keyed by row index and survived
  pagination/tab-switch_ → Save `$set` the wrong document. Now keyed by the document's `_id`
  (`docKey`), resolved back to the doc by `_id` against the current results at save time (skips any
  no longer loaded), and pending edits are cleared whenever `tab.id`/`tab.results` changes.
- **C4. ✅ RESOLVED (2026-07-12).** _Grid inline edit flattened BSON types_ (Date→string,
  ObjectId→string, Decimal128→string, empty cell→`0`). On cell-edit the grid now fetches the
  document's typed shell source, reads the field's original BSON marker, and re-wraps the new value
  in the matching `{$date}`/`{$oid}`/`{$numberDecimal}`/`{$numberLong}`/`{$uuid}` marker (empty
  clears typed/numeric fields to null, not `0`); the row then refreshes to the canonical stored
  value. Falls back to the lossy value only if the typed source can't be fetched (logged).
  _(TreeView nested-field typed editing — M2 — still uses the heuristic parser.)_
- **C5. ✅ RESOLVED (2026-07-12).** _Invalid aggregation JSON threw in render → app-wide white
  screen._ The `buildPipeline(stages)` call feeding `CodeGenModal` is now wrapped in a try/catch, and
  a top-level `ErrorBoundary` (`components/ErrorBoundary.tsx`, wired in `main.tsx`) now catches any
  render-time throw and offers recovery instead of blanking the app.

## 2. High

- SSH tunnel does no host-key verification → MITM. `sshTunnel.ts:78` — add `hostVerifier`.
- Claude agent auto-runs `delete_many`/`update_many` unconfirmed with a "just do it" system prompt,
  while document/profiler/repo text flows back as an unmediated prompt-injection channel.
  `claude.ts:105,268`. Require confirmation for mass mutations; treat tool results as untrusted.
- **H1. ✅ RESOLVED (2026-07-12).** _`matchedCount` never checked_ — a failed match rendered as a
  successful edit until refresh. The grid inline edit, DocumentEditor save, and TreeView save now
  check `matchedCount === 0` and surface an error + refresh instead of showing a false success.
- **H5. ✅ RESOLVED (2026-07-12).** _Filters were never revived_ — `reviveExtended` ran on the
  update body but not the filter, so `{$date}`/`{$oid}` from dialogs and QueryBuilder date rows
  matched nothing. Now `query.ts`/`mutation.ts` run a `reviveFilter` (revive markers → coerce bare
  hex) on every find/count/distinct/explain/update/delete filter, and `convertObjectIds` skips real
  BSON instances so revived types aren't rebuilt into plain objects. QueryBuilder now emits a
  `{$date}` marker for Date rows (`QueryBuilder.tsx`).
- **H2. ✅ RESOLVED (2026-07-12).** _DocumentEditor couldn't `$unset` removed fields (they
  resurrected) and rewrote BSON regex fields as plain strings._ Save now diffs the original top-level
  keys and `$unset`s removed ones; `printShell` renders `RegExp("pat","flags")` and
  `parseShellDocument` parses it back to a `$regex` marker, so regex fields round-trip losslessly.
- **H6. ✅ RESOLVED (2026-07-12).** _DocumentEditor's plain-JSON fallback silently degraded the whole
  document._ The editor now tracks whether the typed shell source loaded; if it didn't, Save warns
  before writing (which would otherwise flatten dates/ObjectIds to strings).
- **H3. ✅ RESOLVED (2026-07-12).** _BulkToolbar "Export" bypassed the lossless exporter_
  (`JSON.stringify` of lossy store docs). It now routes through `exportImport.exportCollection`
  (`serializeToEJSON`, native save dialog) with a `{_id: {$in: ids}}` filter, so exports round-trip.
- **H4. ✅ RESOLVED (2026-07-12).** _Single-collection JSON import stripped `_id`_, regenerating
  every ObjectId and severing references. Import now preserves `_id` (revived via `reviveExtended`),
  matching the DB-level streaming worker. `exportImport.ts`.
- Closing the Claude panel mid-stream strands the tab (`isStreaming` stuck true, text lost) —
  listeners torn down on unmount. `ClaudePanel.tsx:166`, `AppShell.tsx:85` — register once in `App.tsx`.
- Rules-of-Hooks violation — `useState` after early `return null`. `DocumentTable.tsx:225 vs 353`.
- Re-render storm from no-selector store subscriptions across ~10 components (`DocumentTable.tsx:155`,
  TabBar, ClaudePanel, Sidebar, TopBar, …) — select actions individually.
- Stale-response race in `executeQuery` — rapid pagination resolves out of order. `tabStore.ts:312`
  — add a per-tab request token.
- `getRowId` uses nonexistent `params.rowIndex` / stringifies object `_id`s. `DocumentTable.tsx:553`.

## 3. Medium

Security: CSP allows `'unsafe-inline'` + jsdelivr CDN scripts (`index.ts:156`); `window.open`
handler `shell.openExternal`s any scheme with no allowlist (`index.ts:97`); secrets fall back to
plaintext on disk when safeStorage is unavailable, no warning (`config.ts:58`); preload exposes the
generic `ipcRenderer` (`preload/index.ts:8`); `$where`/`$function` pass through filters unfiltered;
`aggregate` buffers the full result set before slicing → OOM risk (`query.ts:115`).

Correctness: every "Run" fires the query twice (`QueryBuilder.tsx:282` + `setPage`); `valueSetter`
mutates store docs in place (`DocumentTable.tsx:281`); `selectedDocIds` never cleared on page/filter
change → bulk-delete off-screen rows (`tabStore.ts:286`); aggregation-tab `DocumentTable` miswired
(dead view buttons; Refresh runs a `find`) (`MainPanel.tsx:205`); ~~`convertObjectIds` coerces any
24-hex string to ObjectId~~ **✅ RESOLVED 2026-07-12** (now only coerces under `_id`; hex-STRING
fields like `UserId` search correctly, QueryBuilder emits `{$oid}` for explicit ObjectId and no
longer auto-infers ObjectId for non-`_id` fields); `removeAllListeners` nukes other
subscribers (`ClaudePanel.tsx:175`, `Sidebar.tsx:94`); `reviveExtended` fires on legit
marker-shaped data (`serialize.ts:155`); numeric `5.0`→int32 flips under `$jsonSchema` validators.

## 4. Low

Dead code (`documentStore.ts` unused, `onPinnedCellEditingStopped` dup, unused imports at
`DocumentTable.tsx:1`); heuristic type inference mislabels hex/ISO strings; client-side single-page
sort masquerading as server sort; `Long`→`toNumber()` display precision loss; unsigned auto-update
supply chain; `sandbox:false` (knowingly, for electron-trpc); no MinKey/MaxKey/DBRef/Code EJSON
cases; CatMode stale-deps meow.

## 5. To verify

Model ID `claude-sonnet-4-6` (`claude.ts:80`, `claudeHealth.ts:80`) is an unusual undated alias —
confirm against the current model catalog; a wrong alias fails the availability probe.
`claude-haiku-4-5-20251001` is fine.

## 6. What the app already gets right

Context isolation on, nodeIntegration off, webSecurity on, real CSP, `safeStorage` for secrets,
token-gated loopback MCP, ReDoS guards, and the recent EJSON round-trip fix — which holds for the
DocumentEditor happy path, insert/update-many dialog bodies, and JSON/NDJSON `exportCollection`.
The gaps above are the paths that fix didn't reach.

## Suggested fix order

~~C1 (wrong-connection)~~ ✅ → ~~C2 (`$out` bypass)~~ ✅ → ~~grid fix (stable position + state-based
heal + `getRowId`)~~ ✅ → ~~C5 ErrorBoundary~~ ✅ → ~~H5 filter revival~~ ✅ → ~~H4 import _id~~ ✅ →
~~C3 TreeView index-keyed edits~~ ✅ → ~~H1 matchedCount~~ ✅ → ~~C4 inline-edit BSON flattening~~ ✅
→ ~~H2 editor `$unset`/regex~~ ✅ → ~~H6 editor JSON-fallback~~ ✅ → ~~H3 BulkToolbar lossless
export~~ ✅ → **remaining (Medium/Low):** M2–M8 (TreeView typed edits, query sequencing, selection
clearing, …) and the grid memoization/re-render-storm perf work.
_(App-signing / L3 left for last, per request.)_

### Post-review hardening (Codex PARTIAL review, 2026-07-12)

A scoped Codex review of the fix series raised 7 issues; all addressed:
- **C4 fetch-failure was still lossy** — the grid inline edit now ABORTS (alert + revert) instead of
  writing the display value when the typed source can't be fetched, and `coerceEditToOriginalType`
  REJECTS invalid ObjectId/date/decimal/integer/boolean input (and Binary/Timestamp) rather than
  downgrading the field to a string.
- **H6 false-safe** — `sourceLoaded` is now set only when the typed shell source is actually
  installed (not when the user edited before it resolved), and the fetch effect keys on
  `tab.id`/`connectionId` too.
- **Filter coercion missed nested arrays** — `convertObjectIds` now recurses into objects inside
  arrays, so hex `_id`s inside `{$and:[…]}`/`{$or:[…]}` (QueryBuilder multi-row) coerce correctly
  (query/mutation/export).
- **Export not revived / not connection-scoped** — `exportCollection` now runs `reviveFilter` (so a
  Date filter exports the rows it counts) and accepts `connectionId` (threaded from BulkToolbar and
  the export dialog).
- **C1 gap in value search** — `valueSearch` is now connection-scoped end to end (router → action →
  dialog).

### Field-search + release-gate hardening (2026-07-12)

- **M1 (hex-string fields unsearchable)** ✅ — `convertObjectIds` now only coerces 24-hex → ObjectId
  under an `_id` path, so a field storing a hex STRING (e.g. `UserId`) searches correctly;
  QueryBuilder emits `{$oid}` for explicit ObjectId and no longer auto-infers ObjectId for non-`_id`
  fields (Date inference now also requires a full ISO datetime).
- **Export/import fast-path progress** ✅ — the `mongodump`/`mongorestore` paths now emit a live
  running card (a successful `mongorestore` previously showed no card); both import functions guard
  the worker fallback so a failure can't leave a card stuck running, and a zero-file fallback errors
  instead of falsely reporting "Complete".
- **Release-gate Codex review** (BLOCKED → PARTIAL → clear) ✅ — fixed a regression where
  QueryBuilder `in`/`nin` stringified the `{$oid}`/`{$date}` marker to `"[object Object]"` (now types
  each element; regex operators use the literal string); the DocumentEditor is now **read-only until
  the typed source loads** (no lossy edit possible, no save-trap) and `$unset` diffs the loaded
  source's keys; all `exportCollection` callers (incl. the explorer tree) pass `connectionId`.

**All audit Criticals (C1–C5) and Highs (H1–H6) are resolved. Remaining: Medium/Low (M2–M8),
the grid memoization/re-render-storm perf work, and app-signing (L3, deferred).**

---

## Sol pass — forward-looking backlog (GPT-5.6 `/full-review`, post-v0.7.5, 2026-07-12)

Fenced max-effort audit of all 111 tracked source files. The data-integrity C1–C5 / H1–H6 above
are fixed and were excluded. Verdict: **PARTIAL**. This is the working backlog. **Note:** the
*security* items below (SSH host-key, MCP auto-approve, plaintext creds, path traversal) were NOT
part of the v0.7.5 data-integrity fixes — they remain open and several are the top priority.

### Critical
- **SB-C1. Claude/MCP work isn't bound to the initiating tab/connection.** Chat omits
  `connectionId`; MCP access checks resolve the mutable global active connection; stream/tool
  results apply to whichever tab is active when each event arrives → writes hit server A while the
  transcript/docs show tab B. `mongo_delete_many` also permits `{}`. Bind each chat to an immutable
  `{tabId, connectionId}`, derive tool context from an opaque session token, tag every renderer
  event with `tabId`. `claude.ts:198`, `mcp/tools.ts:31,458`, `ClaudePanel.tsx:47,87`.

### High
- **SB-H1. Rollback targets the wrong connection & corrupts BSON** — before-images from lossy
  `find()`, ignores stored `connectionId`, update-rollback only `$set`s (leaves added fields), bulk
  deletes record no before-image. Store lossless EJSON snapshots, run against `entry.connectionId`,
  restore full doc / compute `$unset`. `query.ts:149`, `mcp/tools.ts:423,458,557`.
- **SB-H2. Admin / profiler / single-collection import are outside the per-tab connection+write
  boundary** — admin inputs omit `connectionId`; index create/drop have no read-only check; profiler
  mutations have neither; `importCollection` can drop/insert on the active connection. Require
  `connectionId` + one centralized guard per op. `routers/admin.ts:6,57`, `routers/profiler.ts:7`,
  `routers/exportImport.ts:85`, `exportImport.ts:1292`.
- **SB-H3. MCP still auto-approves every Mango tool** — `permissionMode:'default'` is neutralized by
  `canUseTool` returning `allow` for all `mcp__mango__*`; destructive tools lack confirmation,
  blast-radius caps, and empty-filter refusal. Prompt injection → unrestricted writes on RW
  profiles. (Only `$out`/`$merge` was closed in C2.) `claude.ts:267`, `mcp/tools.ts:435,458`.
- **SB-H4. ✅ RESOLVED (2026-07-12).** _Plaintext credentials reached disk._ `saveConnections` now
  **refuses** to persist (throws, surfaced to the user) when `safeStorage` is unavailable instead of
  writing profiles in cleartext, and writes the connections file mode `0600` (config dir `0700`).
  The macOS mongosh launcher's `.command` file now self-deletes on run (`rm -f "$0"`) so the URI is
  on disk only ~1s, and stale launch files are swept. `config.ts`, `mongosh.ts`. _(Broader
  operational-data perms/redaction = SB-M12, still open.)_
- **SB-H5. ✅ RESOLVED (2026-07-12).** _Chat `sessionId` path traversal._ The service now routes
  every save/load/delete through `sessionFilePath()`, which `resolve()`s within `CHAT_DIR` and
  refuses anything that escapes it; the tRPC `save`/`load`/`delete` schemas now require
  `z.string().uuid()` (ids are always `crypto.randomUUID()`). `chatHistory.ts`,
  `routers/chatHistory.ts`.
- **SB-H6. ✅ RESOLVED (2026-07-12).** _SSH tunnel did no host-key verification (MITM)._ Added a
  `hostVerifier` with trust-on-first-use pinning: the SHA-256 fingerprint is pinned to
  `~/.mango/ssh-known-hosts.json` (mode 0600) on first connect and **rejected on mismatch** with a
  clear MITM message thereafter. `sshTunnel.ts`. _(Follow-up: interactive first-use/rotation approval
  UI instead of silent TOFU.)_
- **SB-H7. ✅ RESOLVED (2026-07-12).** _Editing a connection silently cleared security metadata._
  `saveConnection` now preserves the dialog-omitted fields (`claudeDbOverrides`,
  `databaseCodebasePaths`, `folderId`, and `claudeAccess`) from the existing profile via `?? existing`
  instead of overwriting them with `undefined`; the update replaces with the fully-merged profile.
  `connection.ts`.
- **SB-H8. ✅ RESOLVED (2026-07-12).** _TreeView mixed-type `_id` collision._ Edit identity + React
  keys now use a WeakMap `rowIdFor(doc)` token keyed by the document object (never collides the way
  `String(_id)` does). Since the write filter `{_id}` still can't distinguish an ObjectId from a
  same-text string `_id` (that needs the typed-`_id` sidecar, SB-M4), save now **refuses** when >1
  loaded doc shares the `_id` text rather than writing to the wrong one. `TreeView.tsx`.
- **SB-H9. ✅ RESOLVED (2026-07-12).** _"Edit index" dropped before proving the replacement builds._
  When the edit gives the index a **different name**, the replacement is now built FIRST and the old
  one dropped only after success (a failed build leaves the existing index intact). A same-name
  recreate is inherently destructive, but a failed rebuild now surfaces a clear "the collection is
  currently missing this index" error instead of swallowing it. `CreateIndexDialog.tsx`.
- **SB-H10. Memory-exhaustion paths** — aggregation materializes the full cursor before slicing to
  1000; every streamed export row leaks an `error` listener; CSV buffers all docs (no `csvHeaders`
  from router). `query.ts:169`, `exportImport.ts:926,967`, `routers/exportImport.ts:15`.

### Medium
- **SB-M1.** Aggregation + agg-explain bypass EJSON revival (pipelines sent raw, unlike find
  filters) → typed matches silently fail. `AggregationEditor.tsx:12`, `query.ts:169,183,219`.
- **SB-M2.** Claude-streaming re-render storm: broad store subscriptions, per-token tab-array
  recreation, unmemoized `columnDefs`/scans; grid at two JSX positions (Documents vs Aggregation)
  → remounts. `tabStore.ts:274,395`, `DocumentTable.tsx:223,310`, `MainPanel.tsx:163`. _(the grid/
  perf item we already have queued)_
- **SB-M3.** Migration / dump workers / mongosh build connections from the raw URI, ignoring the
  live tunnel/TLS; `sniHostname` never passed to `MongoClient`. `mongodb.ts:23,33`,
  `migration.ts:212`, `exportImport.ts:1016`, `mongosh.ts:79`.
- **SB-M4.** Lossy display is still authority for IDs / add-row types (24-hex string vs ObjectId
  indistinguishable; add-row inserts Date/ObjectId as strings). Carry a typed-id sidecar.
  `serialize.ts:41`, `query.ts:71`, `DocumentTable.tsx:469`.
- **SB-M5.** CSV formula injection — cells starting `= + - @ \t \r` aren't neutralized.
  `exportImport.ts:884,961`.
- **SB-M6.** DB exports aren't snapshot-clean — reuses `<dir>/<db>`, leaves stale collection files a
  later import resurrects. `exportImport.ts:439,453,1093`.
- **SB-M7.** Linked-codebase scan blocks the main thread, no total-byte/time budget.
  `codebaseContext.ts:50,110`, `claude.ts:162`.
- **SB-M8.** Claude abort-controller race — A's `finally` clears the global slot after B replaced it
  → B unabortable. `claude.ts:53,204,380`.
- **SB-M9.** QueryBuilder "Run" fires **two** queries (`setPage(0)` already calls `executeQuery`,
  then `handleRun` again). `QueryBuilder.tsx:286`, `tabStore.ts:286`. _(known M-item, sharper here)_
- **SB-M10.** Profiler state/logs mix connections sharing a db name (cache keyed by db only, no
  `connectionId`). `QueryProfiler.tsx:34,197`, `MainPanel.tsx:84`, `queryLog.ts:28`.
- **SB-M11.** MCP parses 10 MB JSON bodies before token/Origin checks → unauthenticated CPU DoS from
  any local process. Move gates before `express.json`, lower the limit. `mcp/server.ts:50,53,77`.
- **SB-M12.** Operational data (changelog before-images, filters, chat, query history) persisted
  plaintext with default modes. Apply `0700`/`0600`, retention, optional redaction.
  `constants.ts:4`, `changelog.ts:37`, `chatHistory.ts:47`, `queryHistory.ts:37`.
- **SB-M13.** `setWindowOpenHandler` → `shell.openExternal` with no protocol allowlist (the explicit
  IPC handler restricts, this one doesn't). `index.ts:97` vs `:367`.

### Low
- **SB-L1.** Query-history retention unbounded when all 200 entries pinned (`slice(-0)===slice(0)`).
  `queryHistory.ts:68`.
- **SB-L2.** Linux mongosh fallback returns before `spawn` error fires → never tries the next
  terminal. `mongosh.ts:156,163`.
- **SB-L3.** Saving a connection reconnects the wrong profile when names duplicate (discards returned
  id, re-selects by name). `ConnectionDialog.tsx:47,58`.

Plus the standing **renderer typecheck failure**: `query`/`mutation` tRPC router names collide with
the client's built-in `.query()`/`.mutate()`, poisoning `AppRouter` (189 errors) — rename to restore
renderer type-safety.
